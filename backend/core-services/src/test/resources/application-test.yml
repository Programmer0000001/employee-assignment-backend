#Configuration specific to my local environment
spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  liquibase:
    enabled: true
    change-log: classpath:db/db-changelog.yml

  test:
    database:
      replace: none